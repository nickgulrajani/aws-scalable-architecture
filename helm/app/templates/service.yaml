apiVersion: v1
kind: Service
metadata:
  name: retail-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: retail-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
